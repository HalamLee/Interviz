import SplashScreen from '@components/splash-screen';
import { styled } from 'styled-components';

const SplashScreenTest = () => {
  return (
    <Wrapper>
      <SplashScreen />
    </Wrapper>
  );
};

export default SplashScreenTest;

const Wrapper = styled.div``;
