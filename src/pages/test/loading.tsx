import Loading from '@components/loading/Loading';

const LoadingTest = () => {
  return <Loading size={'small'} width="40px" />;
  // return <Loading size={'big'} width="100%" />;
};

export default LoadingTest;
